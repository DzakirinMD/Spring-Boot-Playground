spring:
  datasource:
    url: 'jdbc:postgresql://localhost:49153/student'
    username: postgres
    password: postgrespw
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true